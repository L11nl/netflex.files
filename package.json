{
  "name": "generator-email-railway-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Arabic email/profile manager with a Railway backend for reading generator.email mailboxes",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "cheerio": "^1.1.2",
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
